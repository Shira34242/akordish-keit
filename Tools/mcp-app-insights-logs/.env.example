# Copy to .env for local `npm run test:connection` use only.
# The MCP server itself reads these from the environment via .mcp.json's ${VAR} expansion —
# set them as real user/system environment variables, do not commit a filled-in .env.

AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
APPINSIGHTS_RESOURCE_ID=
